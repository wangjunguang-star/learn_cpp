#include<iostream>
#include<vector>

namespace utils
{
    float auc(const std::vector<size_t>&y, const std::vector<float>& y_pred, size_t bins=10000) {
        return 1.0;
    }
    
} // namespace utils
